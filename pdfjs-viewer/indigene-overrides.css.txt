/* INDIGENE overrides for the official PDF.js viewer.
   Goal: gesture-only reading — no visible toolbar, sidebar, or buttons.
   Only native two-finger pinch-zoom and scroll remain, plus a thin
   loading progress bar so a big PDF doesn't look crashed while it loads. */

#sidebarContainer,
#secondaryToolbar,
#toolbarViewer {
  display: none !important;
}

#toolbarContainer {
  height: 4px !important;
  min-height: 4px !important;
}

#viewerContainer {
  inset: 4px 0 0 !important;
}

#outerContainer.sidebarOpen #viewerContainer:not(.pdfPresentationMode) {
  inset-inline-start: 0 !important;
}

/* Block the long-press "save/open image" context menu on page canvases */
.page,
.page canvas {
  -webkit-touch-callout: none;
}
